여기까지 오늘의 Killercoda 실습입니다. 수고하셨습니다!

**다음 순서**

개인 VM에서 `minikube start` 후 `myapp` Deployment가 계속 떠 있는지 확인하고, `kubectl get pods -o
wide`로 Pod IP를 캡처해두세요. 다음 2차시에 이 Deployment에 Service를 연결하는 실습이 바로
이어지니, Deployment를 지우지 마세요.

**다음 차시 예고**: 2차시에서는 Service의 세 가지 타입(ClusterIP, NodePort, LoadBalancer)을
비교하고, NodePort로 오늘 확인한 문제를 실제로 해결해봅니다.
