여기까지 오늘의 Killercoda 실습입니다. 수고하셨습니다!

**다음 순서 (필수 제출)**

개인 로컬 VM(cloud-lab)에서 오늘과 동일한 4단계(mkdir → echo → docker run -v ... → curl)를
실행하고, `curl localhost:8080` 결과 화면을 캡처해서 LMS 과제 게시판에 제출해주세요.
**컨테이너는 지우지 말고 그대로 두세요** — 다음 3차시에 이 `webvol` 컨테이너를 그대로 이어서
사용합니다.

**다음 차시 예고**

다음 3차시에서는 오늘 만든 `webvol` 컨테이너의 HTML 파일을 수정했을 때 웹 화면에 실시간으로
반영되는지 직접 확인하고, Bind Mount와 비슷하지만 다른 방식인 **Named Volume**을 비교해봅니다.
