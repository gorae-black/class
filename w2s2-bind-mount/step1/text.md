Bind Mount로 컨테이너와 연결할 VM(이 터미널) 쪽 디렉토리를 먼저 만듭니다. Bind Mount는 VM 쪽
경로가 미리 존재해야 하므로, 항상 이렇게 먼저 만들어두는 습관이 필요합니다.

```
mkdir -p ~/html
```

이 명령어는 특별한 출력 없이 프롬프트가 바로 돌아오면 정상입니다. `~/html` 폴더가 새로 만들어진
것입니다.
