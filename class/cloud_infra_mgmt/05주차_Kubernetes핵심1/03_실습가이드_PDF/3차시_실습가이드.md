# 5주차 3차시 실습가이드
## Endpoints와 라운드로빈 (5주차 마지막 시간)

## 개요

지난 2차시에서 NodePort로 우리 앱에 접속하는 데 성공했습니다. 그런데 그 요청이 실제로 어느 Pod로
전달됐는지 궁금하지 않으셨나요? 오늘은 Service 뒤에서 실제로 벌어지는 일, **Endpoints**와
**라운드로빈**을 확인해보겠습니다. 1차시에서 배운 대로 Service는 라벨로 Pod를 찾는데, 그 결과 실제로
연결된 Pod들의 IP 목록이 바로 Endpoints입니다. 그리고 Service로 요청이 여러 번 들어오면, 뒤에 있는
Pod들에게 순서대로 돌아가며 전달됩니다(라운드로빈).

- 소요시간: 개념 설명 25분 + Killercoda 실습 15분 + VM 과제 안내 5분 + 결과 제출 5분 (총 50분)
- 오늘의 핵심: Endpoints는 Service 뒤에 실제로 연결된 Pod IP 목록이다. Service는 여러 Pod에 요청을
  라운드로빈으로 고르게 분산한다.
- **오늘이 5주차 마지막 시간입니다.** 실습 후 1~3차시 과제를 종합해서 제출하세요.

---

## Part A. Killercoda 실습

LMS > 5주차 > `05_Killercoda_링크`에 있는 3차시 링크로 접속한 뒤, 아래 순서대로 진행하세요.

> **참고**: 이 Killercoda 환경은 매번 새로 시작되는 클러스터입니다. 그래서 먼저 지난 시간에
> 만들었던 `myapp` Deployment와 NodePort Service를 다시 만드는 것부터 시작합니다.
>
> 이 세션에는 **60분 제한**이 있습니다. 실습 중 시간이 종료되어도 당황하지 마세요 — 오늘 실습은
> 짧게 끝나므로 문제 없지만, 만약 중간에 끊기면 LMS의 링크로 다시 접속해 이어서 진행하면 됩니다.

### Step 1. myapp Deployment와 NodePort Service 다시 만들기 (복습)

지난 1~2차시와 같은 `myapp` Deployment를 다시 만들고, NodePort Service까지 연결합니다.

```
cat <<EOF > deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: myapp
spec:
  replicas: 2
  selector:
    matchLabels:
      app: myapp
  template:
    metadata:
      labels:
        app: myapp
    spec:
      containers:
      - name: nginx
        image: nginx
EOF
kubectl apply -f deployment.yaml
kubectl expose deployment myapp --type=NodePort --port=80
```

**실행 결과 예시**

```
deployment.apps/myapp created
service/myapp exposed
```

Pod 2개가 Running 상태가 될 때까지 잠시 기다린 뒤 다음 단계로 진행하세요 (`kubectl get pods`로
확인 가능합니다).

### Step 2. Endpoints로 연결된 Pod IP 확인하기

`kubectl get endpoints myapp`을 치면 지금 이 순간 `myapp` Service에 실제로 연결되어 있는 Pod IP
목록을 그대로 볼 수 있습니다. Pod가 새로 추가되거나 삭제될 때마다 이 목록도 자동으로 갱신됩니다.

```
kubectl get endpoints myapp
```

**실행 결과 예시**

```
NAME    ENDPOINTS                       AGE
myapp   10.244.0.5:80,10.244.0.6:80     30s
```

`ENDPOINTS` 칸에 Pod IP 2개가 콤마로 구분되어 나열되어 있습니다. 이게 바로 Service의 '연결 장부' —
지금 이 Service 뒤에 실제로 연결된 Pod가 정확히 몇 개, 어느 IP인지 보여줍니다.

### Step 3. 반복 curl로 요청 여러 번 보내기

먼저 `kubectl get svc myapp`으로 NodePort 번호를 다시 확인하세요.

```
kubectl get svc myapp
```

확인한 NodePort 번호를 아래 `{NodePort 번호}` 자리에 넣고, curl을 10번 반복해서 요청을 보냅니다.

```
for i in $(seq 1 10); do curl localhost:{NodePort 번호}; done
```

**실행 결과 예시**

```
<!DOCTYPE html>
<html>
...
<h1>Welcome to nginx!</h1>
...
</html>
<!DOCTYPE html>
<html>
...
</html>
(10번 반복 출력)
```

오늘 쓰는 nginx 기본 페이지는 어느 Pod가 응답하든 내용이 똑같아서, 이 curl 결과만 봐서는 어느
Pod가 응답했는지 구분이 잘 안 됩니다. 다음 단계에서 `kubectl logs`로 실제로 어느 Pod가 요청을
처리했는지 확인해보겠습니다.

### Step 4. Pod 로그로 라운드로빈 확인하기

`kubectl get pods`로 두 Pod의 이름을 확인한 뒤, 각각 `kubectl logs`로 로그를 확인해보겠습니다.

```
kubectl get pods
kubectl logs {첫 번째 파드 이름}
kubectl logs {두 번째 파드 이름}
```

**실행 결과 예시**

```
NAME                     READY   STATUS    RESTARTS   AGE
myapp-7d9f8c6b5d-abcde   1/1     Running   0          2m
myapp-7d9f8c6b5d-fghij   1/1     Running   0          2m

10.244.0.1 - - [24/Jul/2026:10:00:01 +0000] "GET / HTTP/1.1" 200 615 "-" "curl/7.88.1"
10.244.0.1 - - [24/Jul/2026:10:00:02 +0000] "GET / HTTP/1.1" 200 615 "-" "curl/7.88.1"
...
```

두 Pod의 로그를 각각 확인하면, 방금 보낸 10번의 요청이 두 Pod에 나뉘어 기록되어 있는 것을 볼 수
있습니다(한 Pod에만 몰리지 않고 번갈아가며 기록됨). 이것이 Service가 여러 Pod에 요청을 라운드로빈
방식으로 고르게 분산한다는 증거입니다.

여기까지가 오늘의 Killercoda 실습입니다. 수고하셨습니다! 오늘로 5주차가 모두 끝났습니다.

---

## Part B. 개인 VM 실습

개인 로컬 VM(cloud-lab)에서 Killercoda와 동일한 명령어를 그대로 실행합니다.

1. 클러스터를 켜고, `myapp` Deployment와 NodePort Service가 떠 있는지 확인합니다(지난 2차시에서
   지우지 않았다면 그대로 남아 있을 것입니다).

   ```
   minikube start
   kubectl get deployment myapp
   kubectl get svc myapp
   ```

2. `kubectl get endpoints myapp`으로 연결된 Pod IP 2개를 확인합니다.

   ```
   kubectl get endpoints myapp
   ```

3. `minikube service myapp --url`로 접속 URL을 확인한 뒤, 반복 curl로 요청을 10번 보냅니다.

   ```
   for i in $(seq 1 10); do curl $(minikube service myapp --url); done
   ```

4. `kubectl get pods`로 Pod 이름 2개를 확인하고, 각각 `kubectl logs`로 로그를 확인해 요청이 두
   Pod에 번갈아 기록됐는지 확인합니다.

   ```
   kubectl get pods
   kubectl logs {첫 번째 파드 이름}
   kubectl logs {두 번째 파드 이름}
   ```

**다음 주 예고**: 6주차에서는 설정값과 비밀번호를 코드에서 분리해서 관리하는 ConfigMap과 Secret,
그리고 데이터를 영구적으로 저장하는 PV/PVC를 배웁니다. 6주차 3차시에는 실제 MySQL 데이터베이스를
K8s에 배포해보는 실습도 있습니다.

---

## 제출 안내 (5주차 종합 제출)

오늘이 5주차 마지막 시간이니, 아래 항목을 한 번에 정리해서 LMS 과제 게시판에 제출하세요.

- **1차시**: `kubectl get pods -o wide` 실행 결과(Pod IP가 보이는 화면) 캡처
- **2차시**: `minikube service myapp --url` URL과 curl 접속 결과 캡처
- **3차시(오늘)**: `kubectl get endpoints myapp` 결과 캡처, 반복 curl 실행 결과 캡처
