# 클라우드인프라관리 — 중간고사 (8주차 3차시)

- 범위: 1주차 ~ 7주차 전체
- 배점: 100점 (Part 1. 이론 40점 + Part 2. kubectl 명령어 서술형 30점 + Part 3. YAML 오류 수정 30점)
- 시험 시간: 40분 (LMS 온라인 시험)
- 유의사항: 서술형 문항은 핵심 개념이 정확히 담기면 정답으로 인정합니다. 표현이 강의와 완전히
  같지 않아도 괜찮습니다.

---

## Part 1. 이론 (40점)

**1. (OX, 4점)** VM은 컨테이너와 달리 자신만의 완전한 Guest OS를 갖는다. ( )

**2. (OX, 4점)** Docker에서 이미지(Image)는 실행 중인 인스턴스이고, 컨테이너(Container)는 그
설계도다. ( )

**3. (OX, 4점)** K8s Secret에 넣은 값은 자동으로 암호화되어 아무도 원래 값을 알아낼 수 없다. ( )

**4. (단답형, 4점)** Docker에서 종료된 컨테이너까지 포함해 전체 컨테이너 목록을 보려면 `docker ps`에
어떤 옵션을 추가해야 하나요?

**5. (단답형, 4점)** Dockerfile에서 이미지 빌드 시점에 한 번 실행되는 명령과, 컨테이너가 시작될 때
실행되는 명령을 각각 지정하는 명령어(Instruction)를 하나씩 쓰세요.

**6. (서술형, 6점)** Bind Mount와 Named Volume의 차이를 설명하세요.

**7. (서술형, 6점)** Pod와 Deployment의 차이를 설명하세요.

**8. (서술형, 6점)** Service가 왜 필요한지, "Pod IP"라는 단어를 포함해 설명하세요.

**9. (서술형, 6점)** PV와 PVC의 역할 차이를 설명하세요.

## Part 2. kubectl 명령어 서술형 (30점, 문항당 6점)

아래 각 상황에 맞는 kubectl 명령어를 쓰세요.

**10.** `nginx` 이미지로 `web`이라는 이름의 Pod를 하나 생성하는 명령어

**11.** `myapp`이라는 Deployment를 NodePort 타입 Service로 노출하는 명령어 (포트 80번)

**12.** `web`이라는 Pod의 상세 정보와 이벤트(오류 진단용)를 확인하는 명령어

**13.** `myapp`이라는 Service에 실제로 연결된 Pod IP 목록을 확인하는 명령어

**14.** `db-config`라는 이름으로, `LOG_LEVEL=info`라는 값을 가진 ConfigMap을 생성하는 명령어

## Part 3. YAML 오류 수정 (30점, 문항당 15점)

각 YAML의 오류를 찾아 **어느 줄이, 왜** 잘못되었는지 쓰고, 올바른 값을 쓰세요.

**15.**

```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: webapp
spec:
  replicas: 3
  selector:
    matchLabels:
      app: webapp
  template:
    metadata:
      labels:
        app: webapp
    spec:
      containers:
      - name: webapp
        images: nginx:1.25
        ports:
        - containerPort: 80
```

**16.**

```yaml
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: mysql-pv
spec:
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: /data/mysql
```

---
*(이 문제지는 학생 배포용입니다. 정답 및 채점기준은 교수자용 별도 문서를 참조하세요.)*
