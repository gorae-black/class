여기까지 오늘의 Killercoda 실습입니다. 수고하셨습니다!

**다음 순서**

0주차에 배포받은 개인 로컬 VM에 접속해서, Docker와 Git이 정상 설치되어 있는지 마지막으로
점검해주세요. (본인 PC 터미널에서 `multipass shell cloud-lab` 실행)

문제가 해결되지 않는다면?

1. `06_로컬VM_과제복습` 폴더의 안내 문서를 다시 확인
2. `07_오류FAQ` 폴더에서 동일 증상 검색
3. 그래도 해결이 안 되면 LMS Q&A 게시판에 오류 메시지 스크린샷과 함께 질문 게시 (48시간 내 답변 원칙)
