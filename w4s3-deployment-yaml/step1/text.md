본격적인 실습에 앞서, 지난 시간 복습으로 `kubectl get nodes`를 한 번 실행해봅니다.

```
kubectl get nodes
```

**실행 결과 예시**

```
NAME           STATUS   ROLES           AGE   VERSION
controlplane   Ready    control-plane   12m   v1.30.1
```

노드가 `Ready` 상태로 정상 표시되면, 지난 시간 배운 클러스터가 그대로 잘 떠 있다는 뜻입니다.
