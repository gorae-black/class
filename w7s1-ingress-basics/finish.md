여기까지 오늘의 Killercoda 실습입니다. 수고하셨습니다!

**다음 순서**

개인 VM에서도 `minikube addons enable ingress`를 실행하고, `kubectl get pods -n ingress-nginx`로
STATUS가 `Running`이 될 때까지 기다린 후 결과를 캡처해서 제출해주세요. 컨트롤러 Pod가 완전히
준비되기까지 시간이 좀 걸릴 수 있습니다.

다음 2차시에는 오늘 켜둔 이 컨트롤러를 이용해 실제 라우팅 규칙을 작성하고 도메인 주소로 접속하는
실습을 합니다.
