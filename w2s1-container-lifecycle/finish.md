여기까지 오늘의 Killercoda 실습입니다. 수고하셨습니다!

**다음 순서 (필수 제출)**

개인 로컬 VM(cloud-lab)에서 오늘과 동일한 순서(run → stop → ps -a → start → ps -a → logs → exec)를
실행하고, `docker ps -a` 결과와 `docker logs` 출력 화면을 캡처해서 LMS 과제 게시판에
제출해주세요.

**다음 차시 예고**

다음 2차시에서는 오늘 마지막에 남긴 문제 — 컨테이너를 지우면 그 안의 데이터도 함께 사라지는 문제 —
를 해결하는 **Bind Mount**를 배웁니다. VM의 디렉토리와 컨테이너를 연결해서, 컨테이너를 지워도
데이터는 VM에 그대로 남도록 만드는 방법입니다.
