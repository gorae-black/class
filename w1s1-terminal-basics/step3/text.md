`nproc`으로 이 가상 서버에 할당된 CPU 코어 수를 확인합니다.

```
nproc
```

**실행 결과 예시**

```
2
```

이 숫자는 여러분의 실제 PC의 코어 수가 아니라, 이 Killercoda 가상 환경에 **할당된** 코어 수입니다 —
가상화 기술이 물리 서버의 자원을 이렇게 정해진 만큼씩 나눠준다는 것을 직접 확인한 것입니다.
